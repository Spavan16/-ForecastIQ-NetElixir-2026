Add smoke tests for 5 previously-untested analytics modules; README first-impression pass

1. tests/test_analytics_modules.py -- covers budget_optimizer.py, monte_carlo.py,
   risk_engine.py, rule_engine.py, and scenarios.py, none of which had any test
   coverage before (chat_engine.py/llm_provider.py/pdf_reporting.py deliberately
   left out -- meaningfully testing those needs mocking an LLM response, out of
   scope for this pass). 10 tests total, same no-pytest/plain-assert convention as
   the existing two test files: channel-param sanity + monotonic response to a
   spend increase (BudgetOptimizer), budget-constraint respect + honest
   infeasibility reporting + fail-loud on bad input (optimize_allocation), clip
   bounds + static fallback (derive_revenue_volatility), worst<=expected<=best
   ordering across revenue/ROAS/every channel + histogram bin accounting (Monte
   Carlo), risk score range + classification-threshold consistency + HHI
   concentration response (RiskIntelligenceEngine), all 5 documented sections
   present + recommendations trace to real channels only (RuleInsightEngine), and
   7 ordered scenarios + documented-default fallback on empty input
   (ScenarioGenerator). All 10 pass on the real (non-mocked) code paths.

2. README.md -- added an "At a Glance" section right after the executive
   overview: what the tool does, the exact run command, the one honest headline
   number (5/6 metric-horizon combos beat naive), and the known weak spots named
   up front, all in the space a judge would actually read before deciding whether
   to keep scrolling. Previously the first concrete "how do I run this" appeared
   ~150 lines down, after a long feature list and architecture tree. Also updated
   the folder-structure tree to list the new test file.
